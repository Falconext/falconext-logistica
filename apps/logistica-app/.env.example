# Copia este archivo a .env y completa los valores. NO subir .env al repo.

# URL del backend (API) desplegado en Vercel.
EXPO_PUBLIC_API_URL=https://falconext-logistica-api.vercel.app/api

# Token PÚBLICO de Mapbox (pk...) para los mapas del WebView.
EXPO_PUBLIC_MAPBOX_TOKEN=
